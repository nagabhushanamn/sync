

// ES5

let trainer = {
    name: 'Nag'
};

// let message="The trainer , "+trainer.name +" killing with .js";

// ES6

let message = ` The Trainer ${trainer.name} , teaching .js`;

let htmlTemplate = `
    <div>
        <h1> ${trainer.name}</h1>
    </div>
`;